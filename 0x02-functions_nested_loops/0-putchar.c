#include <stdio.h>

/**
 * main - Printing alphabet in lowercase.
 * Return: 0 (Success)
 */
int main(void)
{
	char ch;

	/* enter your code */

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

	return (0);
}
