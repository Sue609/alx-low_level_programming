#include <stdio.h>

/**
 * main - Printing alphabet in lowercase.
 * Return: 0 (Success)
 */
int main(void)
{
	char ch;

	/* enter your code */

	for (ch = 'x'; ch <= 'y'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

	/* Execute program */

	return (0);
}
