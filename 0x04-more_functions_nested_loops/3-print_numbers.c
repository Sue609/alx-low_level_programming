#include "main.h"

/**
 * print_numbers - checks for checks for a digit (0 through 9).
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
int x;
for (x = 0; x <= 9; x++)
{
_putchar(x);
}
_putchar('\n');
}
